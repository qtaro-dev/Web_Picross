# AGENT.md

## 基本方針

- このプロジェクトは Web版ピクロスゲームです。
- まずは既存コードを読み、現在の構成・起動方法・不具合箇所を把握してください。
- 既存ファイルを不用意に削除しないでください。
- 大きな作り直しをする前に、現状を動かすための最小修正を優先してください。
- 変更範囲は必要最小限にしてください。

## 実装方針

- HTML / CSS / JavaScript の既存構成を優先してください。
- 既存のUI、ファイル構成、命名をできるだけ維持してください。
- 問題データがある場合は、既存形式を確認してから扱ってください。
- ハードコードを増やさず、問題データや設定はできるだけ分離してください。
- ローカルサーバーで動作確認できる状態を目指してください。

## 動作確認

- まずローカルサーバーで起動できることを確認してください。
- 画面が表示されることを確認してください。
- 問題選択、マス操作、クリア判定がある場合は、それぞれ確認してください。
- 修正後は、どのファイルを変更したか、何を確認したかを簡潔に報告してください。

## 禁止事項

- `.git`、`node_modules`、ビルド成果物、不要な一時ファイルを追加しないでください。
- 既存仕様を確認せずに全面的に作り直さないでください。
- APIキーや個人情報をコードに書き込まないでください。
- 使っていない大きなライブラリを勝手に追加しないでください。